# Agent instructions

Before working in this repository, read `CLAUDE.md` and `SESSION.md`, then
follow any task-relevant pointers in `CLAUDE.md` to their owning source before
acting. For Codex integration work, use the `claude-config-conventions` skill
and treat `codex/PARITY.md#codex-integration-sot` as the durable technical
source of truth.

This file is the thin Codex discovery entry point. Do not duplicate project
rules, current state, or design decisions here; their homes are `CLAUDE.md`,
`SESSION.md`, and `DESIGN.md`. The general entry-point contract is
`CONVENTIONS.md#agent-instruction-entrypoints`.
